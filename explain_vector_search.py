"""
向量搜索原理说明
"""

print("""
=== 向量搜索工作原理 ===

1. 查询向量化
   用户查询："错误代码E01"
   ↓
   嵌入模型转换 → [0.023, -0.156, 0.432, ...] (1536维向量)

2. 索引搜索（核心优势）
   使用 HNSW 索引在向量空间中快速导航
   ↓
   只需要检查少量候选向量，不需要遍历全部

3. 相似度计算
   计算查询向量与候选向量的余弦相似度
   ↓
   返回最相似的 K 个结果


=== 为什么不需要全量读取？ ===

HNSW 索引结构（类似跳表的图结构）：

         入口点
        /      \\
     层级1     层级1
      |          |
     层级2 ---- 层级2
    /   \\      /   \\
   叶子 叶子  叶子  叶子
   (实际向量数据)

搜索过程：
1. 从顶层入口点开始
2. 每层向更接近查询向量的节点移动
3. 到达底层后，在局部范围内精确搜索
4. 返回最相似的K个结果

时间复杂度：O(log n)
空间复杂度：只需要访问 O(log n) 个节点


=== 性能对比 ===

假设有 100 万条文档：

全量扫描：
- 需要遍历：1,000,000 条
- 计算相似度：1,000,000 次
- 时间：约几分钟到几十分钟

向量搜索（HNSW）：
- 需要访问：约 100-1000 条候选
- 计算相似度：约 100-1000 次
- 时间：约 10-100 毫秒

性能提升：100-1000 倍


=== Chroma 的实现 ===

Chroma 使用以下技术：
1. HNSW 索引 - 快速近似搜索
2. 持久化存储 - SQLite + 二进制向量文件
3. 批量处理 - 支持高效的批量操作
4. 元数据过滤 - 可以结合元数据过滤和向量搜索


=== 实际使用场景 ===

小数据量（< 1000条）：
- 向量搜索和全量扫描差异不大
- 主要开销在嵌入模型调用

中等数据量（1000-10万条）：
- 向量搜索开始显现优势
- 搜索时间：10-100ms

大数据量（> 10万条）：
- 向量搜索优势明显
- 搜索时间：100-500ms
- 全量扫描可能需要几分钟
""")